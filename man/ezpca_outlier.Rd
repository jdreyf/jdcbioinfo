% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ezpca_outlier.R
\name{ezpca_outlier}
\alias{ezpca_outlier}
\title{PCA plot with optional outlier detection}
\usage{
ezpca_outlier(
  object,
  pheno.df = NULL,
  name = "pca",
  alpha = 1,
  all.size = NULL,
  facet = NULL,
  title = NULL,
  subtitle = NULL,
  rm.leg.title = FALSE,
  labels = FALSE,
  manual.color = NULL,
  manual.shape = NULL,
  ellipses = FALSE,
  outlier.pval = NULL,
  label.outliers = TRUE,
  plot = TRUE,
  ...
)
}
\arguments{
\item{object}{Matrix-like object with features (e.g. genes) as rows and samples as columns.}

\item{pheno.df}{Data frame with rows as samples and columns as phenotypes.}

\item{name}{Name of file to create. Set to \code{NA} to plot to screen instead of to file.}

\item{alpha}{Transparency, passed to \code{\link[ggplot2]{geom_point}}.}

\item{all.size}{Passed to \code{\link[ggplot2]{geom_point}} \code{size} parameter to give size for all points without
appearing in legend. \code{ggplot2} default is size=2.}

\item{facet}{A formula with columns in \code{pheno.df} to facet by.}

\item{title}{Title text; suppressed if it is \code{NULL}.}

\item{subtitle}{Subtitle text; suppressed if it is \code{NULL} or \code{title} is \code{NULL}. If you'd like a
\code{subtitle} but no \code{title}, set \code{title = ""}.}

\item{rm.leg.title}{Logical indicating if legend title should be removed.}

\item{labels}{Logical, should sample labels be added next to points?}

\item{manual.color}{Vector passed to \code{\link[ggplot2:scale_manual]{scale_colour_manual}} for creating a
discrete color scale. Vector length should be equal to number of levels in mapped variable.}

\item{manual.shape}{Vector passed to \code{\link[ggplot2:scale_manual]{scale_shape_manual}} for creating a
discrete color scale. Vector length should be equal to number of levels in mapped variable.}

\item{ellipses}{Logical; should probability ellipses be drawn?}

\item{outlier.pval}{Numeric p‑value threshold for outlier detection
(e.g., 0.05). If \code{NULL} (default), no outlier ellipse is drawn and
no outlier labels are added.}

\item{label.outliers}{Logical; if \code{TRUE} (default) and \code{outlier.pval}
is not \code{NULL}, labels points outside the ellipse using \code{ggrepel}.
The global \code{labels} parameter is automatically set to \code{FALSE}
when outlier detection is active.}

\item{plot}{Logical; should plot be generated?}

\item{...}{Aesthetics passed to \code{\link[ggplot2:aes_]{aes_string}}.}
}
\value{
Invisibly, a \code{ggplot} object. Its \code{data} element contains
the first two PCs, the original \code{pheno.df} (if supplied), and if
\code{outlier.pval} is used, additional columns \code{mahal_dist2} and
\code{outlier} (logical).
}
\description{
PCA scatter plot of the first two principal components. Optionally draws a
confidence ellipse based on Mahalanobis distance (using the covariance of
the plotted PC scores) and labels points outside this ellipse as outliers.
All original functionality of \code{\link{ezlimmaplot::ezpca}} is retained.
}
\details{
PCA is computed with \code{\link[stats]{prcomp}} (no scaling).
Outliers are defined as points whose squared Mahalanobis distance
\eqn{D^2 = (x-\mu)' \Sigma^{-1} (x-\mu)} exceeds the \eqn{1-p} quantile of
the chi‑square distribution with 2 degrees of freedom. The ellipse is drawn
using the same mean vector \eqn{\mu} and covariance matrix \eqn{\Sigma}
estimated from the plotted PC scores.

When \code{outlier.pval} is provided, a single ellipse (ignoring any
grouping aesthetics) is drawn. The original \code{ellipses} parameter (if
\code{TRUE}) draws separate ellipses per group defined by the aesthetic
mapping; both types of ellipses can appear simultaneously.
}
